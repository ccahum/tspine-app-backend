services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: tspine
      POSTGRES_PASSWORD: tspine123
      POSTGRES_DB: tspine_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
